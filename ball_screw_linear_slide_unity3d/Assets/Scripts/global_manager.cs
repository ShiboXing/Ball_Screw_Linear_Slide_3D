using UnityEngine;

public class global_manager : MonoBehaviour
{
    public static GameObject main_cam;
    // Start is called before the first frame update
    void Start()
    {
        main_cam = gameObject;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
